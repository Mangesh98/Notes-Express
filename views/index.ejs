<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Note's</title>
		<link rel="stylesheet" href="stylesheet/style.css" />
		<script src="https://cdn.tailwindcss.com"></script>
	</head>
	<body>
		<div class="main w-full min-h-screen bg-zinc-900 text-white">
			<div class="forms">
				<form action="/create" method="post" class="p-10">
					<input
						class="w-full rounded-md outline-none block px-5 py-2 bg-zinc-800"
						type="text"
						name="title"
						id=""
						placeholder="Title"
					/>
					<textarea
						class="w-full rounded-md outline-none block px-5 mt-2 py-2 resize-none bg-zinc-800"
						name="details"
						id=""
						placeholder="Write your task details here..."
					></textarea>
					<input
						class="bg-blue-600 mt-2 px-5 py-2 rounded-md cursor-pointer"
						type="submit"
						value="Create Task"
					/>
				</form>
			</div>
			<div class="tasks p-10 flex gap-3 flex-wrap">
				<% if(files.length > 0 ){ %> <% files.forEach(function(val){ %>
				<div class="task min-w-72 px-3 py-4 rounded-md bg-zinc-800">
					<h1 class="text-3xl tracking-tighter"><%= val %>></h1>
					<a class="text-blue-500 inline-block mt-3" href="/file/<%= val %>">Read More</a>
				</div>
				<% }) %> <% } else { %>
				<h3 class="text-zinc-600">No task yet.</h3>
				<% } %>
			</div>
		</div>
	</body>
</html>
